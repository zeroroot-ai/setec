# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore everything by default and re-include only needed files
**

# Re-include Go source files (but not *_test.go)
!**/*.go
**/*_test.go

# Re-include Go module files
!go.mod
!go.sum

# Re-include go:embed assets (the installer's boot-time thin-pool
# provisioner script — internal/installer/thinpool.go embeds it).
!internal/installer/payload/*.sh

# Re-include the license text. `**` above hides it, and both Dockerfiles
# copy it into /licenses so the published images carry it (Apache-2.0 §4(a)).
!LICENSE
