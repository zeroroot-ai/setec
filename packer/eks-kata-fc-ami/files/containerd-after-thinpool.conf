# Baked by zeroroot-ai/setec packer/eks-kata-fc-ami as
# /etc/systemd/system/containerd.service.d/10-setec-thinpool.conf.
# containerd's devmapper snapshotter fails plugin init when the thin-pool
# device is absent, so containerd must not start until the pool exists.
[Unit]
Requires=setec-thinpool.service
After=setec-thinpool.service
